#include <stdio.h>
#include "card.h"

struct board
{
    struct card *c1;
    struct card *c2;
    struct card *c3;
    struct card *c4;
    struct card *c5;
    struct card *c6;
    struct card *c7;
    struct card *f1;
    struct card *f2;
    struct card *f3;
    struct card *f4;
};
